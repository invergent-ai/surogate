// The vision binders and materialiser are templates on the tower's geometry now
// (see api/targets/qwen3_6/vision.h): a target with its own tower instantiates
// them on its own config, which a translation unit could not provide.
