# Open-weight pool (ow1) served via OpenRouter with price-priority routing.
# Workers: glm-5.2 (planner/producer), kimi-k3 (coder),
# deepseek-v4-pro (verifier/reasoner), minimax-m3 (implementer/support).
# The conductor's anonymous capability view is unchanged — only the hidden
# runtime_model behind each position differs, so no retraining is needed.
class: director.agentic.fugu_ale.deployer.FuguAleDeployer
id: fugu-ultra-27b-validation-ow3
executor: local
model: fugu-27b-r2-fixed

config:
  model: fugu-27b-r2-fixed
  conductor_base_url: http://localhost:8011/v1
  pool_binding_path: /home/densemax/work/flavius/surogate/director/manifests/fugu_clean_v1/grpo_pilot_train/current_pool_binding_ow3.json
  max_turns: 120
  task_budget_s: 14400
  record_terminal_session: false
  provider_base_url: https://openrouter.ai/api/v1
